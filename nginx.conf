# Management UI server block
server {
    listen 8080;  # NGINX listens externally on port 8080
    location / {
        proxy_pass http://127.0.0.1:15672;  # Forward to RabbitMQ Management UI
    }
}

# Optional: Prevent HTTP traffic on 5672 (for AMQP protocol)
